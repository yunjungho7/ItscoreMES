---
phase: 01-foundation-observability
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "frontend/package.json",
  "frontend/openapi-ts.config.ts",
  "frontend/src/main.ts",
  "frontend/src/App.vue",
  "frontend/src/api/client.ts",
  "frontend/src/components/common/NotificationToast.vue",
  "frontend/src/composables/useNotification.ts",
  "backend/scripts/extract_openapi.py"
]
autonomous: true
requirements: [P1-2, P1-4]

must_haves:
  truths:
    - "A TypeScript SDK is automatically generated from the FastAPI OpenAPI spec."
    - "Frontend types match backend Pydantic models (SCREAMING_SNAKE_CASE preserved per D-12)."
    - "Uncaught frontend errors trigger a global notification (Toast) or fallback page (D-07)."
    - "API errors (4xx, 5xx) are intercepted and displayed as detailed toasts (D-06)."
    - "Toasts auto-hide after 5 seconds (D-05)."
  artifacts:
    - path: "frontend/openapi-ts.config.ts"
      provides: "SDK generation configuration preserving backend naming"
    - path: "frontend/src/api/generated"
      provides: "Type-safe API client and models"
    - path: "backend/scripts/extract_openapi.py"
      provides: "Static OpenAPI schema extraction"
    - path: "frontend/src/composables/useNotification.ts"
      provides: "Centralized notification state management"
    - path: "frontend/src/components/common/NotificationToast.vue"
      provides: "Toast UI component"
  key_links:
    - from: "frontend/src/main.ts"
      to: "app.config.errorHandler"
      via: "Global error boundary registration"
    - from: "frontend/src/api/client.ts"
      to: "useNotification"
      via: "Fetch interceptor pushing errors to state"
    - from: "frontend/src/App.vue"
      to: "NotificationToast.vue"
      via: "Global component placement"
---

<objective>
Implement type-safe SDK generation and a global error handling/notification system for the frontend.

Purpose: Ensure type safety between backend and frontend while improving user experience during errors.
Output: Generated TypeScript SDK, fetch-native API client, and Vue 3 error boundary/toast system.
</objective>

<execution_context>
@C:/Users/JEONGHO/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/JEONGHO/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/01-CONTEXT.md
@.planning/01-foundation-observability/RESEARCH.md
@frontend/package.json
@frontend/src/main.ts
@frontend/src/App.vue
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type-Safe SDK Generation Setup (Static Extraction)</name>
  <files>frontend/package.json, backend/scripts/extract_openapi.py, frontend/openapi-ts.config.ts, frontend/src/api/generated</files>
  <action>
    1. Create `backend/scripts/extract_openapi.py` to statically export the OpenAPI schema:
       ```python
       import json
       from main import app
       with open('../frontend/openapi.json', 'w') as f:
           json.dump(app.openapi(), f, indent=2)
       ```
    2. Install `@hey-api/openapi-ts` and `@hey-api/client-fetch` as dev dependencies in the frontend.
    3. Create `frontend/openapi-ts.config.ts`:
       - Input: `./openapi.json` (pointing to the extracted file).
       - Output: `src/api/generated`
       - Plugins: `@hey-api/client-fetch`, `@hey-api/typescript`, `@hey-api/sdk`.
       - Configuration: Ensure `SCREAMING_SNAKE_CASE` is preserved for property names (per decision [SDK & Type Safety] > Model Naming).
    4. Update `frontend/package.json` with a `generate-api` script:
       - `"generate-api": "python ../backend/scripts/extract_openapi.py && openapi-ts"`
    5. Run `npm run generate-api` to produce the SDK.
  </action>
  <verify>
    <automated>
      # Verify package installation
      grep -E "@hey-api/openapi-ts|@hey-api/client-fetch" frontend/package.json
      # Run generation script (checks if it fails)
      cd frontend && npm run generate-api
      # Verify generated directory exists
      ls frontend/src/api/generated/models.gen.ts
    </automated>
  </verify>
  <done>TypeScript SDK is successfully generated from a statically extracted OpenAPI schema.</done>
</task>

<task type="auto">
  <name>Task 2: Global Error UI (Interceptors & Boundaries)</name>
  <files>frontend/src/composables/useNotification.ts, frontend/src/components/common/NotificationToast.vue, frontend/src/main.ts, frontend/src/App.vue, frontend/src/api/client.ts</files>
  <action>
    1. Create `useNotification.ts` composable using a reactive array to manage toast state (message, type, status code).
    2. Create `NotificationToast.vue` to display non-blocking toasts.
       - Implement auto-hide logic (5 seconds) per decision [Error Notification UI] > Style.
    3. Setup a fetch interceptor in `frontend/src/api/client.ts` using `@hey-api/client-fetch`:
       - Intercept 4xx and 5xx responses.
       - Push detailed error messages (code + message) to `useNotification` (per decision [Error Notification UI] > Detail Level).
    4. Update `frontend/src/main.ts` to register `app.config.errorHandler` to catch uncaught component-level errors.
    5. Update `frontend/src/App.vue` to include the `NotificationToast` component globally.
  </action>
  <verify>
    <automated>
      # Check for errorHandler in main.ts
      grep "app.config.errorHandler" frontend/src/main.ts
      # Verify toast component exists
      ls frontend/src/components/common/NotificationToast.vue
    </automated>
  </verify>
  <done>Frontend errors and API failures are caught globally and displayed as non-blocking toasts with a 5s auto-hide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Frontend | API responses may contain sensitive error details. |
| User -> UI | User sees error messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Error UI | mitigate | Sanitize error messages displayed to users; avoid leaking backend stack traces in production toasts. |
| T-01-06 | Tampering | Generated SDK | mitigate | Enforce SDK generation as a build step; avoid manual modifications to `src/api/generated`. |
</threat_model>

<verification>
1. Run `npm run generate-api` in frontend (verifies static extraction and SDK generation).
2. Start frontend.
3. Manually trigger a 404 or 500 error from the backend and verify a Toast appears.
4. Verify the Toast auto-hides after 5 seconds.
5. Induce a JS error (e.g., `throw new Error('test')`) in a mounted hook and verify the global error handler catches it.
</verification>

<success_criteria>
- SDK is generated without a running server via `extract_openapi.py`.
- Properties in generated SDK use SCREAMING_SNAKE_CASE where appropriate.
- Toasts appear for API errors and auto-hide after 5s.
- Global error handler provides feedback for JS crashes.
- API client uses the generated SDK with fetch-native.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-observability/01-02-SUMMARY.md`
</output>
